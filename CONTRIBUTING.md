# コントリビューションガイド

## 規定ブランチ定義

- 規定ブランチは、リポジトリ設定のデフォルトブランチです。本セクションが本リポジトリでの定義場所です。
- 明示指定がない場合は `main` を優先し、`main` が存在しない場合は `master` を規定ブランチとして扱います。
- 規定ブランチを変更した場合は、本セクションと `CHANGELOG.md` を更新します。

## 変更手順

1. 規定ブランチからブランチを作成します。
2. ルールファイルを追加または更新します。
3. `CHANGELOG.md` を更新します。
4. 根拠とリスクメモを添えてプルリクエストを作成します（説明文の構成は `.github/instructions/pr.instructions.md`）。

## レビュー要件

- `rules/guardrails-unified.v1.md` の変更はセキュリティ担当とプライバシー担当を各1名以上必須とします。
- 互換性に影響する変更はプロダクトオーナー承認を必須とします。

## 開発プロセス規約

- 初回対応を含むすべての作業は、規定ブランチから新しい作業ブランチを切って開始します。
- 規定ブランチへの直接コミットは禁止します。
- ブランチ名は `feature/`, `fix/`, `docs/`, `chore/` のいずれかで開始します。
- `git add` / `git commit` / `git push` はユーザーが任意に実行し、AIエージェントは実行しません。
  例外は「自律ループ実行モード」中の作業ブランチへの `git add` / `git commit` のみです。
  `git push` はモードを問わず常にユーザーが実行します。
- プルリクエスト作成時に、目的・影響範囲・ロールバック方針を記載します。

### 1ブランチ1目的の原則と例外

1つの作業ブランチでは1つの目的に絞ります。例外は次の2つだけです。

1. **`BACKLOG.md` の複数項目の一括対応** — 関連性の高い小規模な変更に限ります。互いに関連の薄い大規模な
   変更は対象に含めません。
2. **自律ループ実行モード** — 単一の目的を「指示された対象を完成させること」と定義し、配下の複数タスクを
   同一ブランチへ積みます。

どちらの例外でも、対象項目の識別子（`id`）を次の3か所すべてへ列挙します。列挙を伴わずに複数目的を1つの
作業ブランチへ混在させることは、引き続き禁止します。

- ブランチ名（件数が多く列挙しきれない場合は件数と実施日で要約し、内訳をコミット本文へ記載）
- コミットメッセージ本文
- プルリクエスト説明の「変更内容」

### 自律ループ実行モードのブランチ・コミット規約

AIエージェントが人の応答を待たずに複数イテレーションを連続実行する運用です。統制要件は
`rules/guardrails-unified.v1.md` セクション12、適用条件は `CLAUDE.md` の同名節、実行手順は
`.claude/skills/autonomous-loop/SKILL.md` を参照してください。
本セクションはブランチとコミットの運用のみを定義します。

- 本モードは、ユーザーが明示的に開始を指示した場合にのみ適用します。
- ループ全体で長期作業ブランチを1本使用します。ブランチ名は `feature/loop-<YYYYMMDD>-<主題>` 形式とします
  （例: `feature/loop-20260912-records-spec`）。
- 1イテレーション＝1コミットとし、コミットメッセージ本文に対象タスクの識別子（`id`）を列挙します。
- 規定ブランチへの直接コミットは、本モードでも禁止します。ループ開始時に現在ブランチを確認し、
  規定ブランチ上であれば作業ブランチを作成してから開始します。
- コミット前に、変更ファイル一覧と差分を確認し、資格情報・個人情報の混入がないことを検査します。
  混入が疑われる場合はコミットせずループを停止します。
- プルリクエストの作成・push はループ終了後にユーザーが実行します。エージェントはPR説明文の案までを提示します。

### 作業開始チェック

- 変更前に `git branch --show-current` で現在ブランチを確認し、規定ブランチ上で直接変更しないことを確認します。
- ブランチ作成後に再確認し、作業ブランチへ切り替わっていることを確認します。

## エージェント指示ファイルの構成規約

エージェント指示は「共通規約 1ファイル + エージェント固有差分」で構成します。同一の規範を複数ファイルへ
複製しないでください。

| ファイル | 役割 | 編集する変更 |
| --- | --- | --- |
| `.github/copilot-instructions.md` | **全エージェント共通の実行ルールの正本** | セキュリティ要件、開発プロセス要件、品質・信頼性要件、ドキュメント管理要件、出力要件、例外と保守 |
| 同ファイル末尾「Copilot 固有の差分」節 | Copilot にのみ適用 | Copilot の git操作禁止、自律ループ対象外、`pr.instructions.md` の自動適用 |
| `CLAUDE.md` | Claude Code 固有の差分と追加規約 | 共通規約への上書き表、本リポジトリの品質ゲート定義、記録ファイルの権限設定、自律ループ実行モードの適用条件 |
| `.claude/skills/*/SKILL.md` | Claude Code のオンデマンド手順 | 明示指示時にしか使わない手順（自律ループ実行モードの実行手順など） |

構成上の前提:

- ファイル名 `copilot-instructions.md` は GitHub が自動読み込みするパスとして規約で固定されているため変更
  できません。内容の適用範囲が Copilot に限らないことを、同ファイル冒頭に明記しています。
- Copilot は同ファイルを自動読み込みします。Claude Code は `CLAUDE.md` の `@import` 経由で読み込みます。
  どちらも参照リンクの追従に依存しないため、共通規約が読まれない状態は発生しません。
- 共通規約と各エージェント固有規約が矛盾する場合は、**エージェント固有規約を優先**します。
- Claude Code のみを利用する配布先でも、`.github/copilot-instructions.md` は共通規約の実体として必須です。

正本が別ファイルにあるもの:

| 項目 | 正本 | 理由 |
| --- | --- | --- |
| 本リポジトリの品質ゲート定義（実行コマンドと合否基準） | `CLAUDE.md`「本リポジトリの品質ゲート定義」 | 全エージェントで同一コマンドを使うため一元化している |
| 自律ループ実行モードの統制要件 | `rules/guardrails-unified.v1.md` セクション12 | 禁止操作と停止条件は常時読み込みされる必要がある |
| 自律ループ実行モードの実行手順 | `.claude/skills/autonomous-loop/SKILL.md` | 明示指示時にしか使わないため、常時読み込みから外している。Claude Code 固有で Copilot は対象外 |
| PR説明文・コードレビューの言語と構成 | `.github/instructions/pr.instructions.md` | Copilot Chat へはパス限定で自動適用され、Claude Code は生成時に同ファイルを読む |

## 単体テストのローカル実行

本リポジトリには GitHub Actions のワークフローが無く、CIでの自動チェックはありません（導入可否は BACKLOG で判断待ち）。
プルリクエスト作成前に、`CLAUDE.md`「本リポジトリの品質ゲート定義」のコマンド（`npm test` と Markdownlint を含む）を
ローカル実行して指摘を解消してください。

- `npm ci` の後に `npm test` を実行します。Node.js 24 標準の `node:test` を使うため追加の依存は無く、
  ブラウザの導入（`npx playwright install`）・外部通信・`conf/` の実ファイルは不要です。
- テストは `test/` 配下に `*.test.js` として置きます（`src/` と同じ構成で対応させます）。
- 挙動を変える変更、シナリオ種別・設定キーの追加・変更では、同じプルリクエストでテストを追加・更新します。
  種別やキーを README・サンプルへ反映し忘れると `test/docs.test.js` が失敗します。

## Markdownlintのローカル実行

品質ゲートの Markdownlint をローカル実行する際の補足です（CI が無い点は前節を参照）。

- 設定 `.markdownlint-cli2.yaml` はカレントディレクトリから自動読み込みされます。
- Node.js / npx が利用できる環境が前提です。
- VS Code 拡張機能 `DavidAnson.vscode-markdownlint` でも確認できますが、`.vscode/` は `.gitignore` 対象のため
  設定は各自で行います。CLI実行結果が最終判定です。
- リンク切れチェックのローカル実行手順は未定義です（未確認）。確認する場合は
  [lychee](https://github.com/lycheeverse/lychee) を別途インストールし `lychee "**/*.md"` を実行します。

## ルール記述スタイル

- 明確で検証可能な表現を使用します。
- MUST、SHOULD、MAY を一貫して使用します。
- 禁止事項と許可事項の例を示します。
- オーナーと最終レビュー日を記載します。

## ドキュメント構成

| パス | 役割 | 詳細 |
| --- | --- | --- |
| `rules/` | ガードレール定義の正本 | `guardrails-unified.v1.md` |
| `docs/guidelines/` | 複数プロジェクトで再利用するガイドライン | `docs/guidelines/README.md` |
| `docs/records/spec/` | 記録ファイルの記述仕様（唯一の参照元） | `docs/records/spec/FORMAT.md` |
| `docs/records/managed/` | AIエージェント管理の記録本体（手動編集禁止） | `docs/records/README.md` |

## 例外管理

- 例外には理由・期限・責任者を必ず記載します。
- 例外はチャット上のみでなく、必ずリポジトリで追跡します。
